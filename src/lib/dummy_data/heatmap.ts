
export const heatmapData = [
    // generate random data from 2022-05-01 to 2022-05-31
    ...Array.from({ length: 31 }, (_, i) => ({
        date: `2022-05-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
    // generate random data from 2022-06-01 to 2022-06-30
    ...Array.from({ length: 30 }, (_, i) => ({
        date: `2022-06-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
    // generate random data from 2022-07-01 to 2022-07-31
    ...Array.from({ length: 31 }, (_, i) => ({
        date: `2022-07-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
    // generate random data from 2022-08-01 to 2022-08-31
    ...Array.from({ length: 31 }, (_, i) => ({
        date: `2022-08-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
    // generate random data from 2022-09-01 to 2022-09-30
    ...Array.from({ length: 30 }, (_, i) => ({
        date: `2022-09-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
    // generate random data from 2022-10-01 to 2022-10-31
    ...Array.from({ length: 31 }, (_, i) => ({
        date: `2022-10-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
    // generate random data from 2022-11-01 to 2022-11-30
    ...Array.from({ length: 30 }, (_, i) => ({
        date: `2022-11-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
    // generate random data from 2022-12-01 to 2022-12-31
    ...Array.from({ length: 31 }, (_, i) => ({
        date: `2022-12-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
    // generate random data from 2023-01-01 to 2023-01-31
    ...Array.from({ length: 31 }, (_, i) => ({
        date: `2023-01-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
    // generate random data from 2023-02-01 to 2023-02-28
    ...Array.from({ length: 28 }, (_, i) => ({
        date: `2023-02-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
    // generate random data from 2023-03-01 to 2023-03-31
    ...Array.from({ length: 31 }, (_, i) => ({
        date: `2023-03-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
    // generate random data from 2023-04-01 to 2023-04-30
    ...Array.from({ length: 30 }, (_, i) => ({
        date: `2023-04-${i + 1}`,
        value: Math.floor(Math.random() * 24),
    })),
];


